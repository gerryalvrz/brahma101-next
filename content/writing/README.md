# Writing content (Phase C)

Future MDX or markdown posts for `/writing` live here.

Planned integration:

- Add MDX pipeline (or `next/mdx`) and map files in this directory to routes under `/writing/[slug]`.
- Keep structured homepage teaser copy in `src/data/home.ts` until posts exist; then optionally derive teasers from frontmatter.
